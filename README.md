# Westward

## Deployment

Pushes to `main` are uploaded to the cPanel site automatically over SSH.

Required GitHub repository secret:

- `HOSTING_SSH_PRIVATE_KEY`: the private key for the `pandossh` hosting account.
